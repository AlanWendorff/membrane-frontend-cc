import { Grid } from "@mui/material";

const WarningChangeNetwork = () => (
  <Grid
    sx={{ pt: 10, gap: 5 }}
    container
    flexDirection="column"
    alignItems="center"
  >
    <h1>Change network to begin</h1>
  </Grid>
);

export default WarningChangeNetwork;
